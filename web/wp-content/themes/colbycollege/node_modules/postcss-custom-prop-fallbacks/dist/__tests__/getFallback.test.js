'use strict';

var _postcss = require('postcss');

var _postcss2 = _interopRequireDefault(_postcss);

var _getFallback = require('../getFallback');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

test('A correct fallback declaration is created.', () => {
    const decl = _postcss2.default.parse('background-color: var(--background-color);').nodes[0];
    const customProps = {
        '--background-color': 'red',
        '--color': 'white',
        '--link-color': 'blue'
    };

    expect((0, _getFallback.getFallback)({ decl, customProps })).toBe('\n  background-color: red');
});

test('An unfound custom prop returns no fallback.', () => {
    const decl = _postcss2.default.parse('background-color: var(--alternative-background-color);').nodes[0];
    const customProps = {
        '--background-color': 'red',
        '--color': 'white',
        '--link-color': 'blue'
    };

    expect((0, _getFallback.getFallback)({ decl, customProps })).toBe('');
});